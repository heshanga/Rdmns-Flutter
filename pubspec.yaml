name: rdmns_flutter
description: Full Featured Cross Platform App for rdmns.hesn.xyz
publish_to: 'none'
version: 1.1.2+12

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.0.0
  flutter_local_notifications: ^17.0.0
  local_auth: ^2.3.0
  shared_preferences: ^2.2.2
  crypto: ^3.0.3
  device_info_plus: ^10.1.0
  http: ^1.2.0
  url_launcher: ^6.2.5
  permission_handler: ^11.3.0
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
